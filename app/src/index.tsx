import React from 'react';
import ReactDOM from 'react-dom';
import ReactCalendar from './react_calendar/components/ReactCalendar';

ReactDOM.render(
  <React.StrictMode>
    <ReactCalendar />
  </React.StrictMode>,
  document.getElementById('root')
);

